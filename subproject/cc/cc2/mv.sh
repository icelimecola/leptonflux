# ============================================================ rm
rm -r data
# ============================================================ mkdir
mkdir -p ./data
# ============================================================ mv
mv ./*.pdf ./data/
mv ./*.root ./data/